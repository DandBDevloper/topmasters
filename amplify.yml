version: 1
frontend:
  phases:
    preBuild:
      commands:
        - yarn install
    build:
      commands:
        - yarn build     # runs `next build`
  artifacts:
    baseDirectory: .next  # Next.js build output
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*