/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['backend.thetopmasters.com', 'anotherdomain.com','enquiries.estate', '127.0.0.1', 'd3h330vgpwpjr8.cloudfront.net'], // Allowed domains for next/image
    },
  };

module.exports = nextConfig;
